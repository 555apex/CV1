# 第一章加分作业 —— 依赖清单
# 本文件是可复现依赖清单；目标平台为 Windows 64-bit + Python 3.13。
# 不要依赖某台电脑的全局 Python 或 Workbuddy 内置环境，请安装到项目 .venv。
#
# 安装（国内镜像）：
#   python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

numpy==2.5.3
opencv-python==5.0.0.93
matplotlib==3.11.2
