# 执行计划：射影矩阵自编程实现

需求依据：`第一章加分作业.md`（已定稿）　日期：2026-09-17

## 一、已确认的技术决策

| 项 | 决策 |
|---|---|
| 求解方法 | 9 未知量齐次 SVD（numpy.linalg.svd），不固定 h33；默认走 Hartley 归一化 |
| 对照实现 | 固定 h33=1 的 8×8 非齐次解，仅用于等价性对照与 h33=0 反例 |
| 重采样 | 手写逆映射 + 双线性插值；H⁻¹ 用伴随矩阵手写 |
| cv2 白名单 | imread/imwrite/resize/cvtColor/setMouseCallback/绘图；黑名单 getPerspectiveTransform、findHomography、warpPerspective、remap（仅在实验中允许调用一次 warpPerspective 作独立交叉验证，报告中标明） |
| 角点标定 | 主流程读 txt/json；附鼠标交互脚本；点序固定 左上→右上→右下→左下 |
| 目标尺寸 | 四边形对边平均长度估计 + 额外 512×512 对照；像素整点约定 0..W-1 / 0..H-1 |
| 矩阵输出 | *.npy 全矩阵 + 报告内缩略数值表与元信息（shape/dtype/min/max/mean）；不导完整 CSV |
| 数据 | 合成图（解析构造真值 H0）做定量实验；实拍图做定性展示 |

## 二、目录结构

```
ComputerVision - 副本/
├─ 第一章加分作业.md          # 需求依据
├─ 执行计划.md                # 本文件
├─ 报告.md                    # 交付主体
├─ README.md                  # 项目总览
├─ PyCharm使用与操作手册.md     # 分阶段全流程走查（本地学习材料，未随仓库分发）
├─ 学习计划.md                 # 代码阅读路线与核心思想（同上）
├─ tests/test_regression.py   # 6 条回归测试
├─ code/
│  ├─ homography.py    # 归一化 / A构造 / SVD求解 / 8元对照 / 求逆 / 误差 / 诊断
│  ├─ warp.py          # 目标尺寸估计 + 逆映射双线性重采样 + 共线性度量
│  ├─ preprocess.py    # 读图 / 灰度 / 降采样 / 矩阵导出 / 角点读写
│  ├─ synth.py         # 合成正向文档图 + 解析真值 H0 + 生成斜图
│  ├─ experiments.py   # 5 组对照实验 + 指标 json + 验证出图
│  ├─ evaluate_public_samples.py  # SmartDoc 公开样例 PSNR 评估
│  ├─ fetch_smartdoc_sample.py    # 公开数据集分块下载助手
│  ├─ check_env.py     # 环境自检 + 求解器最小验证
│  ├─ pick_corners.py  # 鼠标交互标定工具（无显示器时不用）
│  └─ main.py          # 端到端主流程（CLI）
├─ data/{oblique, reference, corners}/
└─ output/{images, matrices, metrics, preview}/    # 合成数据与公开样例共用的唯一输出目录
```

> 约定：`output/matrices/` 与 `output/preview/` 是可再生的运行产物，已加入 `.gitignore`。
> 公开数据集的原始压缩包（469 MB）不进仓库，用 `code/fetch_smartdoc_sample.py` 按需下载。

## 三、函数接口

```python
# homography.py
normalize_points(pts) -> (pts_n, T)
build_A(src, dst) -> A                       # 2n×9
solve_homography_svd(src, dst, normalize=True) -> H
solve_homography_8dof(src, dst, normalize=False) -> (H, info)
apply_homography(H, pts) -> pts'
invert_homography(H) -> H_inv                # 伴随矩阵手写
reprojection_error(H, src, dst) -> (rmse, per_pt)
homography_distance(H1, H2) -> float         # 尺度+符号无关
diagnose(A, H) -> dict                       # κ(A)、σ8/σ9、det(H)
check_point_configuration(pts) -> [issues]

# warp.py
target_corners(W, H) -> 4×2
estimate_target_size(quad) -> (W, H)
warp_bilinear(img, H_inv, W, H, fill=0) -> (out, valid_mask)
warp_nearest(img, H_inv, W, H, fill=0) -> out
measure_collinearity(points) -> (max_dev, dir, center)
```

## 四、验收指标

| 指标 | 门槛 |
|---|---|
| 合成 4 点重投影误差 | ≤ 1e-9 px |
| 齐次解 vs 8 元解（同尺度下） | 差异 ≤ 1e-12 |
| 解得 H vs 解析真值 H0 | 相对距离 < 1e-6 |
| 直线保持性（网格映射后） | 最大偏离 < 0.5 px |
| 正视图 vs 原图 PSNR | > 30 dB（双线性） |
| h33=0 反例 | 齐次解正确；8 元解 cond(M) ~1e16、重投影误差巨大 |

## 五、执行步骤

| 步骤 | 内容 | 可验产物 |
|---|---|---|
| S0 | 环境自检（numpy/opencv/matplotlib 版本与可导入性） | 控制台报告 |
| S1 | 合成数据：正向文档图 + 解析真值 H0 + 斜图 | data/reference、data/oblique |
| S2 | 求解模块 + 单测（4 点等价性、符号/尺度处理） | 测试输出 + metrics json |
| S3 | 重采样模块 + 单测（直线保持性、双线性 vs 最近邻） | output/images |
| S4 | main 端到端跑通合成数据 | 正视图、npy、metrics |
| S5 | 5 组对照实验 + 出图 | output/metrics、output/images |
| S6 | 实拍图接入（交互标定 → 主流程） | data/corners、正视图 |
| S7 | 撰写报告.md（原理→实现→实验→误差分析→结论） | 报告.md |
| S8 | 可选 .ipynb 串联 | notebook |

## 六、关键风险

| 风险 | 预案 |
|---|---|
| 无图形界面，交互脚本崩 | 主流程走 txt 角点，交互脚本仅本机用 |
| 中文字体缺失 | 合成图用 ASCII + 几何图形，图注英文 |
| 实拍纸张未完全入镜 | 加载时做角点越界检查，越界即报错提示重拍 |
| SVD 解的符号任意 | 统一 ‖H‖_F=1，与真值比较时取 ± 最小距离 |
| 插值双重使用导致"自证" | 合成图生成与重采样虽然同核，额外用 cv2.warpPerspective 做一次独立交叉验证 |

## 七、关键理论补充（本计划的推导结论，将写入报告）

1. **作业文档 A_i 第 2 行第 3 个元素必须为 0**。由 $y'(h_{31}x+h_{32}y+h_{33})=h_{21}x+h_{22}y+h_{23}$ 移项所得方程中不含 $h_{13}$，故其系数为 0。若误写为 1，A 仍是 8×9、SVD 仍能返回一个"解"，属静默错误，只能靠重投影误差发现。
2. **8 自由度 ≠ 9 自由度**：$H$ 是齐次等价类，几何自由度恒为 8；$\vec h$ 有 9 个分量。$h_{33}=1$ 是用 1 个尺度自由度做的规范化约定（gauge fixing）。
3. **n=4 且非退化时，$h_{33}=1$ 与齐次 SVD 给出同一个 H**。设 $A=[M|c]$，则 $M$ 奇异 ⟺ 零空间向量第 9 分量为 0 ⟺ 真实 $h_{33}=0$，此时 $h_{33}=1$ 约定失效、无解；齐次 SVD 不受影响。
4. **真正决定精度的是坐标归一化**：像素坐标下 $A$ 的元素跨 1~$10^6$ 量级，$\kappa(A)$ 可达 $10^6$~$10^7$；Hartley 归一化后降到 $O(10)$。非齐次解若用正规方程还会把条件数平方。
5. **合成数据的真值 $H_0$ 必须解析构造**（如 $P=\begin{pmatrix}1&0&0\\0&1&0\\p&q&1\end{pmatrix}$ 与旋转/缩放相乘），不能用本项目的求解器反推，否则实验自证。
