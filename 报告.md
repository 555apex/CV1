# 第一章加分作业报告：射影矩阵的自编程实现

> 代码目录：`code/`　结果目录：`output/`　数据目录：`data/`
> 运行环境：Python 3.13 + numpy 2.5.3 + opencv-python 5.0.0 + matplotlib 3.11.2
> 复现命令：`python code/main.py --synth --experiments && python code/main.py --batch`

---

## 0. 完成情况总览

| 作业要求 | 完成情况 |
|---|---|
| 1. 图像预处理：斜向图片、降分辨率、得出像素矩阵、标定四角点 | 完成。合成 3 组斜视图（含解析真值）+ 支持实拍图；输出分辨率、像素矩阵（`.npy`）、四角点坐标（`.json`） |
| 2. 算法原理解析：自编程实现射影矩阵求解 | 完成。手写 Hartley 归一化、$A$ 矩阵构造、SVD 齐次求解、$h_{33}=1$ 非齐次对照解；核心求解不调用任何 OpenCV 单应函数 |
| 3. 输出要求：输出分辨率、像素矩阵、四角点坐标、正图象素矩阵 | 完成。见第 5 节，所有量均落盘并附数值表 |

**核心指标摘要**

| 指标 | 结果 | 门槛 |
|---|---|---|
| 4 点精确解重投影误差 | $1.7\times10^{-13}$ px | ≤ $10^{-9}$ px |
| 解得的 $H$ 与解析真值 $H_0$ 的相对距离 | $5.6\times10^{-16}$ | < $10^{-6}$ |
| 与 OpenCV `warpPerspective` 的 PSNR | 59.3 / 59.2 / 65.8 dB | > 40 dB |
| 直线保持性（网格映射最大偏离） | $\sim10^{-13}$ px | < 0.5 px |
| $h_{33}=0$ 反例 | 齐次解 3.0e-13 px 正确，8 元解 1.10 px 失效 | 记录在案 |

---

## 1. 图像预处理

### 1.1 数据来源

作业允许"开放数据集或自行拍摄"。本项目采用**两条并行的数据来源**：

1. **合成斜视图（定量主实验）**：先按 A4 比例生成一张类文档正向图（600×800，含网格、棋盘格、同心圆、ASCII 文字、四角标记），再用**解析构造**的单应矩阵真值 $H_0$ 正演得到斜视图。
2. **实拍斜视图（定性展示）**：把手机拍的斜角文档照放入 `data/oblique/`，用 `code/pick_corners.py` 交互标定四角即可跑同一套流程。

> 为什么必须有合成数据：真实照片只能"看着像"，无法回答"解出的 $H$ 误差到底是多少"。合成数据的真值 $H_0$ 是解析构造的（见 1.3），不含任何求解过程，因此是真正独立的参照。

### 1.2 预处理流程

```
读图（兼容中文路径）→ 灰度化 → 按最长边等比降采样 → 得到像素矩阵 → 标定四角点
```

- 降采样用 `cv2.INTER_AREA`，是"简化运算"的常规手段；本流程默认 `--max-side 700`，也可设 `0` 关闭。
- **关键约定**：角点坐标一律以**预处理后（降采样后）的像素坐标系**为准，点序固定 **左上 → 右上 → 右下 → 左下**。若角点是在原图标定的，程序按 `scale` 自动换算。这两种约定混用是这类作业最常见的静默错误来源。
- 像素采用**整点约定**：宽 $W$ 的图像 $x\in[0,W-1]$，高 $H$ 的图像 $y\in[0,H-1]$。

### 1.3 解析真值 $H_0$ 的构造

$$
H_0 = A_{out}\cdot P\cdot R\cdot A_{in}
$$

| 分块 | 含义 |
|---|---|
| $A_{in}$ | 文档像素坐标 → 中心化归一化坐标 |
| $R$ | 绕中心的旋转（本实验取 $-7^\circ$、$11^\circ$、$-5^\circ$） |
| $P=\begin{pmatrix}1&0&0\\0&1&0\\p&q&1\end{pmatrix}$ | 透视（keystone），$\det P=1$ 恒非奇异 |
| $A_{out}$ | 归一化坐标 → 斜视图画布像素坐标（缩放 + 平移，使四边形居中） |

全程只有矩阵乘法。斜视图由 `cv2.warpPerspective`（独立实现）生成，便于后续交叉验证我们手写的重采样。

三组合成数据的参数：

| 名称 | 画布 | $p$ | $q$ | 旋转 | 噪声 $\sigma$ | 文档渲染比例 |
|---|---|---|---|---|---|---|
| `synth_oblique` | 700×820 | 0.30 | 0.18 | $-7^\circ$ | 2.0 | $\approx0.83\times$（降采样，含混叠） |
| `synth_oblique_hard` | 720×800 | $-0.42$ | 0.34 | $11^\circ$ | 4.0 | $\approx0.79\times$ |
| `synth_oblique_clean` | 900×1100 | 0.25 | 0.15 | $-5^\circ$ | 0（无噪声） | $\approx1.15\times$ |

### 1.4 预处理输出

以 `synth_oblique` 为例（`--max-side 700`）：

| 量 | 值 |
|---|---|
| 原始分辨率 | 700 × 820 |
| 降采样系数 | 0.8537 |
| **预处理后分辨率** | **598 × 700** |
| **四个角点坐标**（预处理后坐标系） | $P_0=(49.453,\,40.976)$、$P_1=(534.716,\,84.428)$、$P_2=(548.108,\,559.850)$、$P_3=(204.869,\,659.024)$ |
| 斜图象素矩阵 | shape `(700,598)`，dtype `uint8`，min/max/mean = 0 / 255 / 239.54 |
| 退化检查 | 无任意三点共线 |

完整矩阵见 `output/matrices/synth_oblique_oblique_gray.npy`；降采样数值表见 `output/preview/synth_oblique_oblique_preview.csv`。

**斜图象素矩阵 8×8 缩略（区域平均）**

|  | c0 | c1 | c2 | c3 | c4 | c5 | c6 | c7 |
|---|---|---|---|---|---|---|---|---|
| r0 | 240 | 238 | 240 | 240 | 241 | 243 | 253 | 253 |
| r1 | 253 | 228 | 223 | 225 | 236 | 236 | 241 | 243 |
| r2 | 254 | 235 | 221 | 228 | 243 | 251 | 251 | 244 |
| r3 | 254 | 237 | 186 | 146 | 249 | 251 | 251 | 245 |
| r4 | 254 | 247 | 201 | 186 | 244 | 236 | 223 | 245 |
| r5 | 254 | 254 | 238 | 251 | 247 | 237 | 234 | 245 |
| r6 | 254 | 254 | 239 | 234 | 234 | 238 | 245 | 248 |
| r7 | 254 | 254 | 245 | 245 | 249 | 254 | 254 | 254 |

前 3 行 × 前 8 列的原始灰度值：

```
[[255 255 255 255 255 254 255 254]
 [254 254 254 254 254 255 255 254]
 [254 254 254 255 253 254 255 255]]
```

---

## 2. 算法原理

### 2.1 射影变换与自由度

对应关系 $\tilde{x}' = H\tilde{x}$，展开为

$$
x'_{i}=\frac{h_{11}x_{i}+h_{12}y_{i}+h_{13}}{h_{31}x_{i}+h_{32}y_{i}+h_{33}},\qquad
y'_{i}=\frac{h_{21}x_{i}+h_{22}y_{i}+h_{23}}{h_{31}x_{i}+h_{32}y_{i}+h_{33}}
$$

**关于自由度必须区分两件事**（作业文档已按此口径修正）：

- $H$ 是**齐次等价类**（$H$ 与 $\lambda H$ 表示同一个射影变换），几何自由度恒为 **8**；
- 待解向量 $\vec h=(h_{11},\dots,h_{33})^T$ 有 **9 个分量**。

$h_{33}=1$ 就是利用那 1 个多余的尺度自由度所做的**规范化约定（gauge fixing）**。9 是未知数个数，8 是独立自由度。

### 2.2 线性方程组 $A\vec h=0$ 的构造

交叉相乘消去分母，整理成齐次线性方程组，每对点贡献 2 行：

$$
A_i=\begin{pmatrix}
x_{i}&y_{i}&1&0&0&0&-x_ix'_i&-y_ix'_i&-x'_i\\
0&0&0&x_{i}&y_{i}&1&-x_iy'_i&-y_iy'_i&-y'_i
\end{pmatrix}
$$

$n=4$ 时 $A$ 是 $8\times9$ 矩阵。

> **作业文档的一处笔误（已修正）**：文档原稿中 $A_i$ 第 2 行第 3 个元素写成了 1，应为 **0**。推导：由 $y'(h_{31}x+h_{32}y+h_{33})=h_{21}x+h_{22}y+h_{23}$ 移项所得方程中不含 $h_{13}$，故其系数必为 0。这一点非常关键——因为 $A$ 恒为 $8\times9$（8 行 < 9 列），**无论 $A$ 写得对不对，SVD 都一定会返回一个"解"**，属于静默错误，只能靠重投影误差发现。第 1 行整体取负与标准式等价（$\vec h$ 的零空间不变），所以第 1 行的符号约定无碍。

### 2.3 齐次 SVD 求解（主实现）

对 $A$ 做奇异值分解 $A=U\Sigma V^T$，取 $V$ 的**最后一列**（对应最小奇异值的右奇异向量）作为 $\vec h$，重塑为 $3\times3$ 得 $\tilde{H}$。

理论依据：$\|\vec h\|=1$ 约束下最小化 $\|A\vec h\|$，其解正是 $A^TA$ 最小特征值对应的特征向量；而 $A^TA=V\Sigma^2V^T$，所以该解即 $V$ 的最后一列。

### 2.4 归一化 DLT（数值稳定）

直接对像素坐标做 DLT 对尺度极敏感：$A$ 的元素同时含常数 1 与 $x\cdot x'\sim10^6$ 量级，条件数 $\kappa(A)\approx1.1\times10^6$。Hartley 归一化流程：

1. **平移**：点集质心移至原点
2. **缩放**：到原点的平均欧氏距离缩放到 $\sqrt{2}$（归一化后坐标 RMS 恰为 1，是条件数最优取值）
3. 对归一化后的点执行标准 DLT
4. **反归一化**：$H=T'^{-1}\tilde{H}T$

实测把 $\kappa(A)$ 从 $1.09\times10^6$ 降到 **3.59**（见表 exp2），重投影误差从 $9.7\times10^{-10}$ 降到 $1.7\times10^{-13}$。

### 2.5 $h_{33}=1$ 的非齐次解（对照实现）

记 $A=[M\,|\,c]$（$M$ 为前 8 列，$c$ 为第 9 列），固定 $h_{33}=1$ 后方程变为 $M\vec x=-c$。

**两者关系（本项目的核心理论结论）**：$M$ 奇异 $\iff$ 零空间向量的第 9 分量为 0 $\iff$ 真实 $h_{33}=0$。

- 若 $M$ 可逆，则对任意 $h_{33}$ 都有 $\vec x=-M^{-1}c\,h_{33}$，解空间是 1 维的，$h_{33}=1$ 一定可行 —— 此时**两种解法给出同一个 $H$**；
- 若真实 $h_{33}=0$，则不存在 $h_{33}=1$ 的解，$M$ 必奇异，该约定**结构性失效**，而齐次 SVD 不受影响。

因此本作业**主实现采用 9 未知量齐次 SVD**，$h_{33}=1$ 只作为对照与反例演示。

### 2.6 最小点数要求

- 每对点提供 **2 个独立方程**；$H$ 有 **8 个自由度**
- 因此至少需要 **4 对对应点，且其中任意 3 点不共线**（存在 3 点共线则 $A$ 秩亏，$H$ 不可解）
- $n>4$ 时用 SVD 求最小二乘解，提高鲁棒性

---

## 3. 自编程实现说明

### 3.1 模块结构

| 文件 | 职责 |
|---|---|
| `code/homography.py` | 核心：归一化、$A$ 构造、SVD 齐次解、8 元非齐次对照解、手写伴随矩阵求逆、重投影误差、数值诊断、退化检查 |
| `code/warp.py` | 手写逆映射 + 双线性插值、最近邻对照、目标尺寸估计、固定目标角点、共线性度量 |
| `code/preprocess.py` | 兼容中文路径的读写、灰度化、降采样、角点读写、矩阵导出与缩略表、PSNR |
| `code/synth.py` | 正向文档图生成、解析真值 $H_0$、斜视图生成、噪声注入、数据集构建 |
| `code/experiments.py` | 4 组对照实验、管线验证、指标 json、出图 |
| `code/main.py` | 端到端 CLI（单张 / 批量 / 建数据 / 跑实验） |
| `code/pick_corners.py` | 鼠标交互角点标定工具 |

### 3.2 自编程边界（"哪些是手写的"）

**手写实现（本作业的算法本体）**
- $A$ 矩阵构造、Hartley 归一化与反归一化
- SVD 齐次求解的完整流程（含零空间选取、尺度规范化）
- $h_{33}=1$ 非齐次解
- $3\times3$ 逆矩阵（伴随矩阵法，未调用 `np.linalg.inv`，实测与 `np.linalg.inv` 差 $4.5\times10^{-13}$）
- **整个图像重采样**：逆映射 + 双线性插值（含齐次除法、有效掩膜、越界填充）
- 重投影误差、齐次尺度与符号无关的 $H$ 距离、共线性度量

**允许调用的原语**
- `numpy.linalg.svd`：奇异值分解属于线性代数原语，不是本作业的算法本体
- `cv2.imread/imwrite/resize/cvtColor/setMouseCallback/绘图`：预处理与可视化
- 实验中额外调用一次 `cv2.warpPerspective` 作为**独立实现的交叉验证**（已在结果中明确标注用途），交付流程不使用它

**未使用（黑名单）**：`cv2.getPerspectiveTransform`、`cv2.findHomography`、`cv2.remap` 在 `code/` 中**完全没有出现**；`cv2.warpPerspective` 仅出现在 `synth.py`（生成合成数据）与 `experiments.py`（独立交叉验证与出图）中，**交付算法路径 `warp.py` / `homography.py` 未使用**（可用 `grep -rn "warpPerspective\|getPerspectiveTransform\|findHomography\|remap" code/` 核验）。

### 3.3 为什么用逆映射

前向映射（把每个源像素搬到目标位置）在缩放/斜视下会在目标图留下空洞。逆映射对**每个目标整数像素**反算源坐标：

$$
\tilde{s}=H^{-1}\tilde{u},\qquad s=(s_x/s_w,\ s_y/s_w)
$$

然后双线性采样

$$
I(s)=(1-d_x)(1-d_y)I_{00}+d_x(1-d_y)I_{01}+(1-d_x)d_yI_{10}+d_xd_yI_{11}
$$

处理要点：
- $|s_w|<\varepsilon$ 的点位于无穷远（或"背后"）→ 标为无效像素，填充背景色
- 源坐标超出图像范围 → 填充背景色
- 目标尺寸默认由四边形**对边平均长度**估计（$W=\frac{|p_1p_0|+|p_2p_3|}{2}$，$H$ 同理），保持宽高比、不额外引入各向异性缩放；同时输出 512×512 固定尺寸对照

### 3.4 工程细节（踩坑记录）

1. **中文路径**：Windows 下 `cv2.imread/imwrite` 遇中文路径会失败（本项目目录名含"副本"），统一改用 `np.fromfile + cv2.imdecode` / `cv2.imencode + tofile`。
2. **半像素约定**：`cv2.resize` 采用像素中心约定，与"整点约定 $0..W-1$"不一致，约 0.4 px 的系统性错位会让文字/网格这类高对比边缘的 PSNR 显著虚低。因此参考图用**按整点约定的仿射映射**生成（`cv2.warpAffine` + $s_x=(W-1)/(W_0-1)$）。
3. **坐标系一致性**：解得的 $H$ 定义在降采样后的坐标系、真值 $H_0$ 定义在原图坐标系，两者直接比较会得到 $10^{-2}$ 量级的假误差。正确做法是先换算 $H_{orig}=H\cdot\mathrm{diag}(s,s,1)$ 再比（本项目已实现在 `main.py`）。
4. **齐次尺度与符号歧义**：SVD 解出的 $\vec h$ 整体符号任意，与真值比较须取 $\min(\|H-H_0\|,\|H+H_0\|)$（本项目 `homography_distance`）。

---

## 4. 实验结果

### 4.1 实验 1：4 点情形下两种解法的等价性

对应点由解析真值 $H_0$ 施加于文档四角得到（无噪声、非退化）。

| 解法 | 重投影 RMSE (px) | 与真值 $H_0$ 的距离 | $\kappa$ 或 $\mathrm{cond}(M)$ |
|---|---|---|---|
| 齐次 SVD + Hartley 归一化 | $1.675\times10^{-13}$ | $5.62\times10^{-16}$ | 3.59 |
| 齐次 SVD（不归一化） | $9.748\times10^{-10}$ | $7.33\times10^{-12}$ | $1.085\times10^{6}$ |
| $h_{33}=1$ 非齐次解 | $7.200\times10^{-9}$ | $3.17\times10^{-11}$ | $1.085\times10^{6}$ |

- 两种解法的 $H$ 距离为 $3.17\times10^{-11}$，**在各自精度内等价**（9 元解精度 $\sim\varepsilon\kappa\approx2\times10^{-16}\times10^6\approx10^{-10}$，与实测吻合）。
- 结论：$n=4$ 非退化时二者给出同一个 $H$；9 元解精度更高，且不需要 $h_{33}\neq0$ 的隐含假设。

### 4.2 实验 2：归一化的作用

| 设置 | $\kappa(A)$ | Δ 重投影 RMSE | Δ 与真值距离 |
|---|---|---|---|
| 原始像素坐标 | $1.085\times10^{6}$ | $9.75\times10^{-10}$ px | $7.33\times10^{-12}$ |
| Hartley 归一化 | **3.59** | **$1.67\times10^{-13}$** px | **$5.62\times10^{-16}$** |

条件数下降 5 个量级，精度提升约 3 个量级。见 `output/images/fig_exp2_normalization.png`。

### 4.3 实验 3：$h_{33}=0$ 的反例（结构性失效）

真值取 $H_0=\begin{pmatrix}1&0&0.3\\0&1&0.2\\10^{-3}&10^{-3}&0\end{pmatrix}$，$\det\approx-1.61\times10^{-4}\neq0$（合法射影变换，但 $h_{33}=0$，源坐标原点被映到无穷远，故点集避开原点）。

| 解法 | 重投影 RMSE | 与真值距离 | 其他 |
|---|---|---|---|
| 齐次 SVD | $2.98\times10^{-13}$ px ✅ | $6.96\times10^{-13}$ | $H[2,2]=9.8\times10^{-16}$（正确复现 $h_{33}=0$） |
| $h_{33}=1$ 非齐次解 | **1.104 px** ❌ | 1.414（已完全错误） | $\mathrm{cond}(M)=2.0\times10^{17}$，线性残差 6.49，`consistent=False` |

同时 `normalize_h(H,'h33')` 会明确抛错：*"H[2,2] ≈ 0：该 H 把源点原点映到无穷远，无法用 h33 归一化"*。

**结论**：$h_{33}=1$ 是一个**有条件成立**的约定；本项目选择 9 未知量齐次 SVD 是有实际意义的，不只是形式上的"更一般"。

### 4.4 实验 4：$n>4$ 加噪声的最小二乘鲁棒性

50 次重复，对应点均匀取自文档范围内、仅在目标点上叠加高斯噪声（单位：像素）。

**表 A：重投影 RMSE（px）**

| $\sigma$ (px) | $n=4$ | $n=5$ | $n=8$ | $n=20$ | $n=40$ |
|---|---|---|---|---|---|
| 0 | $1.28\times10^{-13}$ | $1.47\times10^{-13}$ | $1.60\times10^{-13}$ | $1.76\times10^{-13}$ | $1.82\times10^{-13}$ |
| 0.5 | $1.28\times10^{-13}$ | $2.68\times10^{-1}$ | $4.48\times10^{-1}$ | $6.41\times10^{-1}$ | $6.76\times10^{-1}$ |
| 2.0 | $2.41\times10^{-13}$ | $1.207$ | $1.983$ | $2.443$ | $2.676$ |

**表 B：解得的 $H$ 与真值 $H_0$ 的距离（尺度与符号无关）**

| $\sigma$ (px) | $n=4$ | $n=5$ | $n=8$ | $n=20$ | $n=40$ |
|---|---|---|---|---|---|
| 0 | $7.24\times10^{-14}$ | $3.76\times10^{-15}$ | $2.86\times10^{-15}$ | $2.62\times10^{-15}$ | $2.41\times10^{-15}$ |
| 0.5 | $1.334\times10^{-1}$ | $3.908\times10^{-2}$ | $1.072\times10^{-2}$ | $4.685\times10^{-3}$ | $3.449\times10^{-3}$ |
| 2.0 | $2.273\times10^{-1}$ | $9.712\times10^{-2}$ | $6.573\times10^{-2}$ | $2.393\times10^{-2}$ | $1.416\times10^{-2}$ |

两个值得强调的结论：

1. **$n=4$ 时重投影误差失去鉴别力**：精确解使得 4 个点被完全拟合，RMSE 即使加了噪声也几乎为 0（过拟合）。此时必须用表 B 的"$H$ 与真值的距离"衡量——这正是 $\sigma=0.5$、$n=4$ 时 RMSE 仅 $1.3\times10^{-13}$ 而 $H$ 距离却达 0.133 的原因。
2. $H$ 距离随 $n$ 单调下降（$\sigma=0.5$ 时由 $1.3\times10^{-1}$ 降到 $3.4\times10^{-3}$），说明**增加对应点数能有效抑制噪声**，齐次 SVD 最小二乘按预期工作。见图 `output/images/fig_exp4_noise.png`。

### 4.5 实验 5：端到端管线验证

| 用例 | 目标尺寸 | 角点重投影 RMSE | 与真值 $H$ 的距离 | PSNR vs `cv2.warpPerspective` | PSNR vs 真值 $H$ 重采样 | PSNR vs 参考图 |
|---|---|---|---|---|---|---|
| `synth_oblique` | 495×652 | $1.13\times10^{-13}$ | $4.7\times10^{-15}$ | 59.28 dB | 270.9 dB | 26.29 dB |
| `synth_oblique_hard` | 473×624 | $2.07\times10^{-13}$ | $1.1\times10^{-15}$ | 59.23 dB | 271.3 dB | 23.51 dB |
| `synth_oblique_clean` | 689×910 | $2.44\times10^{-13}$ | $2.4\times10^{-16}$ | 65.75 dB | 268.8 dB | 30.23 dB |

**直线保持性**（文档平面上的水平/竖直/斜线各取 40 点，经映射后拟合直线的最大垂距）：

| 用例 | 真值映射 (px) | 经本实现解出的 $H$ (px) |
|---|---|---|
| `synth_oblique` | $9.0\times10^{-14}$ ~ $1.5\times10^{-13}$ | $9.6\times10^{-14}$ ~ $2.8\times10^{-13}$ |
| `synth_oblique_hard` | $3.1\times10^{-14}$ ~ $9.6\times10^{-14}$ | $8.9\times10^{-14}$ ~ $1.5\times10^{-13}$ |

结果图：
- `output/images/fig_*_overview.png`：斜图（含标定角点）/ 本实现正视图 / 合成参考图 三栏对比
- `output/images/fig_*_grid.png`：把规则网格经 $H$ 映射到斜视图，直线仍为直线
- `output/images/fig_*_interp.png`：最近邻 / 双线性 / OpenCV 三种重采样的整体与局部放大对比
- `output/images/fig_exp2_normalization.png`、`fig_exp4_noise.png`：实验 2、4 的图

**插值质量**：最近邻 vs 双线性的 PSNR 为 24.4~27.2 dB，差距集中在文字与细线边缘（见局部放大图），双线性明显更平滑，符合预期。

**关于"PSNR vs 参考图"只有 23.5~30.2 dB 的说明**（这不是算法误差）：
- 噪声本身的 PSNR 上限：$\sigma=2$ 时 $10\log_{10}(255^2/4)=42.1$ dB，$\sigma=4$ 时 36.1 dB；
- 剩余差距来自**信息损失链**：文档在斜视图中被以 $\mathrm{INTER\_LINEAR}$ 降到 $0.79\sim0.83$ 倍，对 1 像素细线产生混叠；随后正视图又要放大回接近原分辨率。这是"两次重采样 + 降采样混叠"的固有损失；
- 无噪声且在 $1.15\times$ 下渲染的 `synth_oblique_clean` 达到 **30.23 dB**，验证了上述归因。
- 真正衡量实现正确性的指标是"vs `cv2.warpPerspective`"（59.3~65.8 dB）与"vs 真值 $H$ 重采样"（$\approx270$ dB，即与用解析真值求得的正视图**数值上无法区分**）。后者把求解误差与外插值误差彻底隔离：两者共用同一套重采样代码，唯一差别是 $H$ 来自求解还是来自解析真值。

---

## 5. 输出要求对应（分辨率 / 像素矩阵 / 四角点 / 正视图矩阵）

以 `synth_oblique`（`--max-side 700`）为例，全部输出均可复查：

| 要求 | 输出 | 位置 |
|---|---|---|
| 预处理后分辨率 | 598 × 700 | `output/metrics/<name>_run.json` → `preprocess` |
| 斜图象素矩阵 | `(700,598)` uint8，min/max/mean = 0/255/239.54 | `output/matrices/<name>_oblique_gray.npy` |
| 四个角点坐标 | $P_0$(49.453, 40.976)、$P_1$(534.716, 84.428)、$P_2$(548.108, 559.850)、$P_3$(204.869, 659.024) | 同上 json → `corners_in_processed_coords` |
| 反射矩阵 $H$ | 见下 | json → `H.h33_normalized` |
| 正图象素矩阵 | `(556,422)` float64，min/max/mean = 0/255/230.16 | `output/matrices/<name>_rectified_gray.npy` |

**射影矩阵 $H$（斜视图 → 正视图，$h_{33}$ 归一化）**

```
[[ 6.169218e-01, -1.551325e-01, -2.415185e+01],
 [ -5.653876e-02,  6.314167e-01, -2.307667e+01],
 [ -5.207653e-04, -3.127878e-04,  1.000000e+00]]
```

数值诊断：$\kappa(A)=3.591$，$\sigma_8/\sigma_9=1.238$，$\det H=9.798\times10^{-6}$，$\mathrm{cond}(H)=2.23\times10^{3}$，角点重投影 RMSE $=1.089\times10^{-13}$ px，与真值 $H$ 的相对距离 $=4.475\times10^{-15}$。与不归一化解的距离 $2.37\times10^{-12}$、与 8 元解的距离 $2.51\times10^{-12}$（均与其各自精度相符）。

**正视图像素矩阵 8×8 缩略**

|  | c0 | c1 | c2 | c3 | c4 | c5 | c6 | c7 |
|---|---|---|---|---|---|---|---|---|
| r0 | 218 | 212 | 213 | 219 | 220 | 227 | 239 | 223 |
| r1 | 229 | 228 | 234 | 239 | 251 | 251 | 251 | 236 |
| r2 | 218 | 189 | 201 | 246 | 251 | 251 | 251 | 235 |
| r3 | 212 | 120 | 142 | 251 | 251 | 245 | 245 | 236 |
| r4 | 230 | 234 | 239 | 251 | 240 | 223 | 223 | 223 |
| r5 | 235 | 251 | 251 | 249 | 242 | 224 | 220 | 228 |
| r6 | 232 | 236 | 236 | 238 | 240 | 241 | 243 | 234 |
| r7 | 220 | 225 | 228 | 225 | 231 | 239 | 239 | 223 |

前 3 行 × 前 8 列的插值结果（可见亚像素插值产生的非整数值）：

```
[[ 67.7  23.7  42.8  51.2  51.7  44.4  61.6 212.3]
 [ 54.5   0.4   0.0   0.0   0.1   2.0 118.9 249.9]
 [ 53.8   0.9   1.4   1.0   0.5   5.2 141.7 252.3]]
```

三组数据的完整输出（斜图/正视图 PNG、`.npy`、缩略 CSV、run.json）均已在 `output/` 下生成。

---

## 6. 误差分析与讨论

**误差来源分解**（按本项目的度量链条）

| 环节 | 量级 | 说明 |
|---|---|---|
| 求解误差（齐次 SVD + 归一化） | $10^{-13}$ px | 4 点精确情形，主要由浮点运算决定 |
| 未归一化的额外误差 | $10^{-9}$ px | 条件数 $10^6$，$\varepsilon\kappa\approx10^{-10}$ |
| $h_{33}=1$ 路径的额外误差 | $10^{-8}$ px（$n=4$）/ 1.1 px（$h_{33}=0$） | 后者是完全失效 |
| 角点输入误差的传播 | 每像素角点误差 $\Rightarrow$ 同量级 $H$ 扰动 | $\sigma=0.5$ px 时 4 点解的 $H$ 距离达 0.13 |
| 插值误差 | PSNR 受限的主要因素 | 双线性较最近邻提升 24~27 dB 的差距 |
| 数据固有信息损失 | 参考图 PSNR 上限 | 斜视图降采样混叠 + 两次重采样 |

**几点结论**

1. **归一化是精度的第一决定因素**，其影响（3 个量级）远大于"8 元 vs 9 元"的选择（在同一尺度下两者差异 $3\times10^{-11}$，量级即为 8 元解自身精度）。
2. **$h_{33}=1$ 不是等价改写，而是条件约定**。真实 $h_{33}=0$ 时它无解。虽然实际拍照场景中 $h_{33}=0$ 属边缘情形，但从算法正确性角度，齐次 SVD 是更稳妥的选择。
3. **重投影误差不是万能的验收指标**。$n=4$ 时它恒为 0（精确解），有噪声时更是过拟合的表征。必须配合"$H$ 与真值的距离"和"输出的图像一致性"。
4. **坐标系与约定的一致性容易被忽视**：本次实现中先后遇到"降采样坐标系 vs 原图坐标系"和"半像素约定"两个坑，两者都会带来数量级上被夸大的误差，但与算法本身无关。

---

## 7. 结论

- 用 $4$ 对（或更多）对应点自编程求解射影矩阵的完整链路已实现并验证：$A$ 构造 → Hartley 归一化 → SVD 齐次求解 → 反归一化 → 手写逆映射重采样 → 正图象素矩阵输出。
- 在 4 点精确情形下重投影误差 $1.7\times10^{-13}$ px，解出的 $H$ 与解析真值的相对距离 $5.6\times10^{-16}$；端到端输出与 OpenCV 独立实现的一致性达 59~66 dB，与"真值 $H$ 重采样"的一致性达 $\approx270$ dB（数值上不可区分）。
- 通过 4 组对照实验定量说明了归一化 DLT 的必要性、$h_{33}=1$ 约定的适用边界、以及 $n>4$ 最小二乘在抗噪上的作用。

**局限**
- 4 点输入对单点标定误差敏感，无鲁棒估计（未实现 RANSAC）；真实照片建议标定 6~8 个点。
- 仅处理平面单应，未建模镜头径向畸变；强畸变下需先做畸变校正。
- 输出正视图的尺寸由对边平均长度估计，与真实的物理宽高比可能存在百分之几的偏差（本实验估计 495×652，理论 600/800 比例为 0.75，估计值 0.759）。

---

## 8. 复现步骤与文件清单

```bash
# 1) 生成合成数据（正向文档图 + 解析真值 H0 + 三张斜视图 + 角点文件）
python code/main.py --synth

# 2) 跑全部对照实验（含出图与指标 json）
python code/main.py --experiments

# 3) 批量跑端到端流程（输出正视图、npy 矩阵、缩略表、run.json）
python code/main.py --batch --max-side 700

# 4) 接入实拍图：交互标定四角 → 跑主流程
python code/pick_corners.py --image data/oblique/my_photo.jpg --max-side 700 \
                            --out data/corners/my_photo.json
python code/main.py --image data/oblique/my_photo.jpg \
                    --corners data/corners/my_photo.json --max-side 700
```

```
├─ 第一章加分作业.md              # 需求依据（已定稿）
├─ 执行计划.md                    # 技术决策、验收指标、执行步骤
├─ 报告.md                        # 本文档
├─ code/
│  ├─ homography.py               # 核心求解（自编程）
│  ├─ warp.py                     # 手写逆映射 + 双线性
│  ├─ preprocess.py               # 预处理与矩阵导出
│  ├─ synth.py                    # 合成数据与解析真值
│  ├─ experiments.py              # 4 组对照实验 + 管线验证
│  ├─ pick_corners.py             # 交互标定
│  └─ main.py                     # 端到端 CLI
├─ data/
│  ├─ reference/                  # doc_gray.png + <name>_H0.json（解析真值）
│  ├─ oblique/                    # 斜视图（合成 3 张；实拍图可放入）
│  └─ corners/                    # 角点坐标 json
└─ output/
   ├─ images/                     # 斜图、正视图、最近邻、512×512、实验图
   ├─ matrices/                   # *.npy 完整像素矩阵
   ├─ preview/                    # *.csv 降采样数值表
   └─ metrics/                    # experiments.json + 每张图的 run.json
```
